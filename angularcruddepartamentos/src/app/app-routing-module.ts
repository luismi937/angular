/*
  Módulo de enrutamiento: angularcruddepartamentos
  - Aquí se definen las rutas para las páginas de CRUD de departamentos.
  - Actualmente el arreglo de rutas está vacío; añadir rutas según componentes disponibles.
*/
import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

const routes: Routes = [];

@NgModule({
  imports: [RouterModule.forRoot(routes)],
  exports: [RouterModule]
})
export class AppRoutingModule { }
